<%= form_for(@audio) do |f| %>
  <% if @audio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@audio.errors.count, "error") %> prohibited this audio from being saved:</h2>

      <ul>
      <% @audio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :artifact_id %><br />
    <%= f.select(:artifact_id, Artifact.find(:all).collect { |artifact| [artifact.name, artifact.id] } ) %>
  </div>
    <div class="field">
    Audio<br />
    <%= f.file_field :mp3 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
